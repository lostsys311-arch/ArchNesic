#!/bin/bash
# ArchNesic — automated post-boot customization
# Runs once on first boot if enabled at build time.

# Nothing to persist — all changes are discarded on reboot.
# This file exists to maintain archiso compatibility.
true
